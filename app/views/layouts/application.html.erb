<!DOCTYPE html>
<html>
<head>
  <title>Mortgage Affordability</title>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
  
  <!-- Optional theme -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.5/cosmo/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav>
    <ul class="nav nav-tabs my-nav navbar-fixed-top">
      <li role='presentation' class='link1'><a href="#">Home</a></li>
      <li role='presentation' class='link2'><a href="#prev-search">Searches</a></li>
      <li role='presentation' class='link3'><a href="#new-search">New Search</a></li>
      <% unless @user %>
        <li style="float:right"><a href="#" class="sign-in">Sign In</a></li>
      <% else %>

        <li style="float:right"><a href="#user-details">User</a></li>
        <li style="float:right"><a href="/users/signout" class="sign-out">Sign Out</a></li>
      <% end %>
    </ul>
  </nav>
  <% flash.each do |key, value| %>
    <% if key == "notice" %>
      <%= content_tag :div, value, class: "alert-msg alert alert-info" %>
    <% elsif key == "alert" %>
      <%= content_tag :div, value, class: "alert-msg alert alert-danger" %>
    <% else %>
      <%= content_tag :div, value, class: "alert-msg alert alert-success" %>
    <% end %>
  <% end %>

<%= yield %>

<footer></footer>
</body>
</html>
